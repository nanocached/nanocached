[package]
name = "velox"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"

[dependencies]
tokio = { version = "1", features = ["io-util", "macros", "net", "rt", "sync"] }

[lints.clippy]
all = "deny"
