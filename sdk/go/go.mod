// Interim module path (the repository still lives under t0k0sh1; see the
// nanocached GitHub organization for where it may move — moving AFTER a
// tagged release would be a breaking change for importers, which is why
// the call gets made before v1).
module github.com/t0k0sh1/nanocached/sdk/go

go 1.22
